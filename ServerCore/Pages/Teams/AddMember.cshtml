@page "/{eventId}/{eventRole}/Teams/AddMember"
@model ServerCore.Pages.Teams.AddMemberModel

@{
    ViewData["Title"] = "Add Member";
    //Needs route data - ViewData["AdminRoute"] = "/Teams/AddMember";
    //Needs route data - ViewData["AuthorRoute"] = "/Teams/Status";
    //Needs route data - ViewData["PlayRoute"] = "/Teams/AddMember";
    Layout = "_teamLayout.cshtml";
}

<h2>@Html.DisplayFor(model => model.Team.Name): Add member</h2>

<div>
    <a asp-page="./Members" asp-route-teamId="@Model.Team.ID">Cancel</a>
</div>

<h4>Select a user to add to this team:</h4>

<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Users[0].ID)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Users[0].Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Users[0].Email)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model.Users)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.ID)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Name)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Email)
                </td>
                <td>
                    <a asp-page-handler="AddMember" asp-route-teamId="@Model.Team.ID" asp-route-userId="@item.ID">Add</a>
                </td>
            </tr>
        }
    </tbody>
</table>