namespace ServerCore.DataModel
{
    /// <summary>
    /// The answer submission to an single player puzzle (puzzles that can be solved as an individual player rather than a the team).
    /// </summary>
    public class SinglePlayerPuzzleSubmission : SubmissionBase
    {
    }
}
